import styled from 'styled-components';

export const Avatar = styled.span`
	img {
		// width: 30px;
		// height: 30px;
		// // width: ${(props) => (props.big ? '25px' : '20px')};
		// // height: ${(props) => (props.big ? '25px' : '20px')};
        // border-radius: 50%;
        width: 25x;
		height: 25px;
		img {
			margin: -10px;
		}
	}
`;
